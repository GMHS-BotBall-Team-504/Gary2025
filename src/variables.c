#include "../include/variables.h"

// Initialize the wheel ports with corresponding port numbers
WheelPorts wheels = {
    .frontLeft = 1,    // Port 1
    .frontRight = 0,   // Port 0
    .backLeft = 3,     // Port 3
    .backRight = 2     // Port 2
};